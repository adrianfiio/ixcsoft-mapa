# AFService Map v0.65.0 — Editor Óptico v2

Backup/ponto de rollback desta rodada: tag `v0.64.0` (também disponível
como branch `backup/mapa-pre-v0100-20260801`). Pacote preparado pelo
ChatGPT para a área do mapa ("v0.10.0" na numeração dele) e aplicado aqui
via `apply_v0100.py` (validado antes com `--dry-run`).

## Principais mudanças

### Adicionado

- **Inserir caixa direto no cabo**: ao clicar num cabo, a opção agora é
  "Inserir caixa" — abre uma janela compacta pra criar uma CTO/CDO/CEO
  nova ou conectar uma já existente, escolher entre cortar (dois
  segmentos) ou registrar passagem sem cortar, definir a capacidade da
  CTO e arrastar a prévia da caixa sobre o cabo antes de confirmar (só
  salva depois de confirmado; cancelar não grava nada). Pra ligar dois ou
  mais cabos na mesma caixa, basta repetir a inserção em cada cabo
  escolhendo "Conectar caixa existente" e selecionando a mesma
  CTO/CDO/CEO. O corte continua usando a rotina segura já existente
  (bloqueia cabo com fusões ou terminações); se a caixa for criada mas o
  corte falhar, o sistema desfaz a caixa automaticamente, sem deixar
  ponto abandonado no projeto.
- **Passagem sem corte**: novo endpoint autenticado pra registrar que um
  cabo passa por uma CEO/CDO/CTO sem cortar — o cabo passa a aparecer na
  caixa e pode ser cortado depois, direto na tela de fusões.
- **Linhas de fusão editáveis**: modo "Editar linhas" — clique pra
  selecionar a linha, arraste os pontos de dobra, duplo clique adiciona
  um ponto, botão direito num ponto remove, "Ortogonal" reorganiza a
  ligação, "Automático" remove o desenho manual, "Excluir" usa a exclusão
  original da ligação. O caminho fica salvo no mesmo layout JSON já
  existente (sem migration) e continua lá ao fechar e abrir de novo.
- **Cores por tipo de ligação**: fibra óptica em ciano, cobre/RJ45 em
  laranja, wireless em roxo tracejado (fibras mantêm suas cores reais);
  linha selecionada ganha destaque visual.

### Corrigido

- **Editor de fusões menor e móvel**: ocupa menos espaço, pode ser
  arrastado pela barra do título, mantém o mapa visível atrás, cabeçalho
  mais compacto e mais área útil pro diagrama — continua com tela cheia,
  desmaximizar e zoom com Ctrl + rolagem.

## Verificação feita nesta rodada

- `--dry-run` do aplicador rodado antes da aplicação real, sem erro.
- `python -m py_compile` no arquivo novo (`optical_editor_v2.py`, teste
  `test_optical_editor_v2.py`) e no `urls.py` tocado.
- Sintaxe de `map-optical-editor-v2.js` (novo), `map-editor.js` e
  `map-fusion-polish.js` verificada com esprima.
- Chaves de `map-optical-editor-v2.css` balanceadas; `{% if %}`/
  `{% endif %}` de `templates/map.html` balanceados.
- Aplicador rodado duas vezes: segunda rodada não gerou nenhuma alteração
  adicional (idempotente, sem duplicar código).
- Leitura do novo endpoint `register_cable_passage`
  (`optical_editor_v2.py`): exige `IsAuthenticated`, confere
  `can_edit_company(request.user, element.company_id)` e bloqueia quando
  o cabo e a caixa não pertencem à mesma empresa/projeto — mesmo padrão
  de segurança já usado em `cut_cable_at_element`.

**A suíte Django completa (`manage.py check`, `manage.py test`) e os
testes end-to-end do `docs/TEST_PLAN_V0100.md` dependem de banco e
navegador reais — não rodam neste ambiente de preparação.**

## Atualização

Execute `apply` no servidor. Sem migration nesta versão.

Teste sugerido (roteiro completo em `docs/TEST_PLAN_V0100.md`): inserir
CTO/CDO/CEO nova num cabo (com corte e com passagem sem corte), conectar
uma caixa já existente a um segundo cabo, tentar cortar um cabo com fusão
existente (deve bloquear), abrir o editor de fusões e testar arrastar a
janela pela barra do título, entrar em "Editar linhas" e mover/adicionar/
remover pontos de dobra numa ligação, usar "Ortogonal" e "Automático",
fechar e reabrir a tela de fusões confirmando que o caminho editado
continua salvo, e conferir as cores por tipo de ligação (fibra ciano,
cobre laranja, wireless roxo tracejado).
