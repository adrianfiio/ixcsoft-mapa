# AFService Map v0.8.5

## Identidade oficial e Django Admin

### Alterado

- Nome do produto atualizado para **AFService Map**.
- Logotipo `afservice-logo.png` aplicado no dashboard, mapa e administração.
- Título da API atualizado para AFService Map API.

### Corrigido

- Todos os arquivos estáticos passaram para `/assets/static/`, evitando a
  interceptação do Nginx em `/static/`.
- CSS do Django Admin restaurado.
- O comando `apply` valida o CSS do dashboard, o CSS do Admin e o logotipo antes
  de informar sucesso.

O nome IXCSoft permanece somente nas áreas relacionadas à integração com o ERP.
