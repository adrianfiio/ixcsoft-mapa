# AFService Map v0.12.5

## Correções

- O editor impede selecionar uma fibra que já participa de outra ligação.
- Entradas e saídas de splitter já conectadas orientam a excluir primeiro a linha atual.
- O motivo do bloqueio aparece no topo do próprio unifilar, não apenas no menu lateral.
- As portas agora aparecem como `S1`, `S2` etc., indicando “saída do splitter”.
- Fibras dos cabos continuam identificadas como `F1`, `F2` etc.
- O popup grande do cabo fecha automaticamente ao abrir a edição ou mover o traçado.

## Atualização

```bash
apply
```

Depois recarregue com `Ctrl+F5`.
